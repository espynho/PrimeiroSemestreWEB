"use strict";
let p1 = new Pessoa("Fábio", 33, 1.70); // objeto criado na classe Pessoa, com o método construtor
let p2 = new Pessoa("Alcides", 40, 1.69);

console.log(p1.toString()); // chamando método dentro da classe Pessoa que foi especificada(o caminho) no html
console.log(p2.toString());