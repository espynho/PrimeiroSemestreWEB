"use strict"
let base, altura, area ;

base = parseInt(prompt("Entre com a base do triangulo"));

altura = parseInt(prompt("Entre com a altura do triangulo"));

area = (base*altura)/2;

alert(area);

document.write(area);

